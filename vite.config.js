import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  base: '/verve-management-events/', // Set the base path for GitHub Pages
  plugins: [react()],
  base: '/verve-management-events/'
})